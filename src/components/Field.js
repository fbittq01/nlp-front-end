import InputBase from '@mui/material/InputBase';
import { styled } from '@mui/material/styles';

const CustomizedField = styled(InputBase)({
  border: '1px solid #333',
  fontSize: 16,
  borderRadius: '20px',
  padding: '0.5rem 1rem',
});

export default CustomizedField;
